{% extends 'base.html.twig' %}

{% block title %}Edit Society{% endblock %}

{% block body %}
    <h1>Edit Society</h1>

    {{ include('society/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('society_index') }}" class="ml-2 mt-5 whitespace-nowrap inline-flex items-center justify-center px-4 py-2 border border-transparent rounded-md shadow-sm text-base font-medium text-white bg-indigo-600 hover:bg-indigo-700">back to list</a>

    {{ include('society/_delete_form.html.twig') }}
{% endblock %}
